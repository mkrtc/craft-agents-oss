# v0.11.11 — Default Role Labels

This file accumulates release notes for the next unreleased version. PRs that add user-visible behavior should append a bullet to the relevant section here. Versioned files (`X.Y.Z.md`) are owned by the release skill — never create them in feature commits.

## Features

## Improvements

- **Default role labels** — New and existing workspaces now get the Executor, Auditor, Designer, and Tester role labels under Workflow when those labels are missing, while preserving user-customized labels and avoiding duplicates.

## Bug Fixes

## Breaking Changes
